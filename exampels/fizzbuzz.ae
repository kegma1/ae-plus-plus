1 når dup 100 <= gjør
    hvis dup dup 5 / slipp 0 = snu 3 / slipp 0 = og gjør
        "fizzbuzz" 
    ellvis dup 3 / slipp 0 = gjør
        "fizz" 
    ellvis dup 5 / slipp 0 = gjør
        "buzz" 
    slutt
    
    hvis dup Helt = gjør
        dup 
    slutt

    skrivnl
    
    1 +
slutt
